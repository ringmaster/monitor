{
    "browserslist": [
        "last 1 chrome versions"
    ],
    "scripts": {
        "dev": "parcel src/index.html",
        "build": "parcel build src/index.html --no-source-maps --experimental-scope-hoisting --out-dir build"
    },
    "devDependencies": {
        "less": "^3.11.1",
        "parcel-bundler": "^1.12.4",
        "parcel-plugin-svelte": "^4.0.6",
        "svelte": "^3.20.1"
    }
}
